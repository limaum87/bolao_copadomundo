__pycache__/
*.pyc
*.pyo
backend/data.db

# Backups do banco (NUNCA commitar — contêm dados de produção)
*.sqlite
*.sqlite3
*.db.bak*
backend/data.db.*

# Imagens avulsas de teste
top3.png
.venv/

# Sync secrets
backend/.env.sync

# Web Push (VAPID) — chaves e .env do projeto (NUNCA commitar)
.env
.env.*
!.env.example
backend/.env.vapid
